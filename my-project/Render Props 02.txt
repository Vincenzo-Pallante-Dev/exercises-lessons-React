Change the `TodoList` component so that instead of using the `render` prop it uses the `children` prop. How does this affect the component in which the `TodoList` component is rendered?
Cambia il componente `TodoList` in modo che invece di usare la prop `render` utilizzi la prop `children`.
 In che modo ciò influisce sul componente in cui viene visualizzato il componente "TodoList"?